import {
  ReactElement,
  JSXElementConstructor,
  ReactNode,
  ReactPortal,
  AwaitedReactNode,
} from 'react'

const PostP = (props) => {
  return <div>{props.paragraph}</div>
}

export default PostP
